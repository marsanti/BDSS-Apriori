## Apriori Algorithm

to run the script:

```bash
python main.py
```

Use the flag `--help` to understand how to change the default configuration.

### Results

Results will be saved by default in the `results/{dataset_name}/{algorithm}_{timestamp}` folder.
The configuration used is saved in a __YAML__ file.